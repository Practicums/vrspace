apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: vrspace-tls
  namespace: default
spec:
  secretName: vrspace-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  commonName: vrspace.endpoints.metaverse-363005.cloud.goog
  dnsNames:
  - vrspace.endpoints.metaverse-363005.cloud.goog